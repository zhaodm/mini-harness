# 添加新 {X}

> 场景: {什么时候需要这个操作（一句话）}

## 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| {path} | 新建 | {desc} |
| {path} | 修改 | {在哪里注册/关联} |
| {path} | 新建 | {测试文件} |

## 前置条件

- {precondition}

## 步骤

1. 创建 `{path}` — 参考: `{existing_similar_file}`
   ```
   {关键代码模板片段，≤10行}
   ```

2. 注册到 `{path}` — 在 {具体位置描述}
   ```
   {注册代码片段}
   ```

3. 添加测试 `{test_path}` — 覆盖: {正常 + 异常}
   ```
   {测试骨架}
   ```

4. ⚠️ 同步更新 `{config_or_doc}` （容易遗漏）

## 验证

```bash
{验证命令，确认操作成功}
```
